import 'package:flutter/material.dart';

// IMPORTACONES QUE NOSOTROS ESCRIBIMOS
import 'package:flutter_vscode3/src/app.dart';
//import 'src/app.dart';

void main() {
  // runApp(new MyApp()); 
  runApp(MyApp()); // lo que esta esperando aca es un widget
}
